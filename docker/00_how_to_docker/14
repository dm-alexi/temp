docker run --restart unless-stopped --name lair -p 8080:80 \
	-e WORDPRESS_DB_HOST=$(docker inspect --format '{{ .NetworkSettings.IPAddress }}' spawning-pool) \
	-e WORDPRESS_DB_PASSWORD=Kerrigan \
	-d wordpress