/*
#include <mlx.h>
#include "fdf.h"

void	window(char *file)
{
	void	*mlx;
	void	*win;

	if (!(mlx = mlx_init()) || !win = mlx_new_window(mlx, 800, 600, file))
		sys_error();
	for (int i = 0; i < 100; ++i)
		mlx_pixel_put(mlx, win, i, 100, 250);
	mlx_loop(mlx);
}
*/
